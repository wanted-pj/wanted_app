<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.techtown.wanted_app_main">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@color/white"
        android:supportsRtl="true"
        android:theme="@style/Theme.Wanted_app_main"
        android:networkSecurityConfig="@xml/xml"
        android:usesCleartextTraffic="true">
        <activity android:name=".Activity.Login.LoginRegisterActivity"/>
        <activity android:name=".Activity.MainActivity"
            android:windowSoftInputMode="adjustPan"/>
        <activity
            android:name=".Activity.Login.LoginActivity"
            android:windowSoftInputMode="adjustPan">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activity.PostingWriteActivity"/>
    </application>

</manifest>